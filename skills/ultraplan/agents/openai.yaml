interface:
  display_name: "Ultraplan"
  short_description: "Review a plan through ChatGPT Pro and a constrained MCP outbox"
  default_prompt: "Use $ultraplan to prepare a sanitized plan review with my approved policy and hand it to ChatGPT Pro. Keep evidence preparation and remote review read-only, then validate the result and resume my original task under its existing authorization."
policy:
  allow_implicit_invocation: false
